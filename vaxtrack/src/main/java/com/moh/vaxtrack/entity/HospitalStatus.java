package com.moh.vaxtrack.entity;

// An ENUM is a fixed list of allowed values.
// A hospital's status can ONLY ever be one of these two things - nothing else.
public enum HospitalStatus {
    ACTIVE,
    INACTIVE
}